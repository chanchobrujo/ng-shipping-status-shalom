export interface ButtonProperties {
  title: string;
  titleColor: string;
  contentColor?: string;
}

export interface InputProperties {
  title: string;
  width: string;
  inputType?: string;
  textColor?: string;
  titleColor?: string;
  contentColor?: string;
}
